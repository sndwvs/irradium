# Description: Xfce Screensaver
# URL: https://docs.xfce.org/apps/screensaver/start
# Maintainer: mara, mara at fail dot pp dot ua
# Depends on: libxss libxklavier libwnck3 garcon dbus-glib python python-gobject

name=xfdesktop
version=4.18.2
release=1
source=(https://archive.xfce.org/src/apps/$name/${version%.*}/$name-$version.tar.bz2)

build(){
  cd $name-$version

  ./configure \
    --prefix=/usr \
    --sysconfdir=/etc \
    --libexecdir=/usr/lib \
    --localstatedir=/var \
    --disable-debug \
    --enable-gtk-doc=no \
    --disable-nls

  make
  make DESTDIR=$PKG install || exit 1

}
