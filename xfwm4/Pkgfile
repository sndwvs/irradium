    # Description: Xfce's window manager
# URL: https://docs.xfce.org/xfce/xfwm4/start
# Maintainer: mara, mara at fail dot pp dot ua
# Depends on: libwnck-gtk3 libxfce4ui libxfce4util startup-notification xorg-libxcomposite xorg-libxpresent

name=xfwm4
version=4.18.0
release=1
source=(https://archive.xfce.org/src/xfce/$name/${version%.*}/$name-$version.tar.bz2 \
        xinitrc.xfce)

build(){
  cd $name-$version

  ./configure \
    --prefix=/usr \
    --libdir=/usr/lib \
    --libexecdir=/usr/lib/$name \
    --sysconfdir=/etc \
    --localstatedir=/var \
    --enable-startup-notification \
    --enable-xpresent \
    --enable-compositor \
    --disable-static \
    --disable-debug \
    --disable-nls

  make
  make DESTDIR=$PKG install || exit 1

  find $PKG -name README -exec rm '{}' \+

  install -D -m755 $SRC/xinitrc.xfce $PKG/etc/X11/xinit/xinitrc.xfce

}
