From 6bd4bdded4fe666dc90872fd4472b967b6e5cd55 Mon Sep 17 00:00:00 2001
From: Jan Wassenberg <janwas@google.com>
Date: Fri, 2 May 2025 01:13:15 -0700
Subject: [PATCH] update RVV version checks to 1.0. Refs #2554

PiperOrigin-RevId: 753903434
---
 hwy/aligned_allocator.cc | 2 +-
 hwy/base.h               | 4 ++--
 hwy/detect_targets.h     | 4 ++--
 hwy/ops/rvv-inl.h        | 2 +-
 4 files changed, 6 insertions(+), 6 deletions(-)

diff --git a/hwy/aligned_allocator.cc b/hwy/aligned_allocator.cc
index e857b2288f..de0ef549d2 100644
--- a/deps/v8/third_party/highway/src/hwy/aligned_allocator.cc
+++ b/deps/v8/third_party/highway/src/hwy/aligned_allocator.cc
@@ -28,7 +28,7 @@ namespace hwy {
 namespace {
 
 #if HWY_ARCH_RISCV && defined(__riscv_v_intrinsic) && \
-    __riscv_v_intrinsic >= 11000
+    __riscv_v_intrinsic >= 2000000
 // Not actually an upper bound on the size, but this value prevents crossing a
 // 4K boundary (relevant on Andes).
 constexpr size_t kAlignment = HWY_MAX(HWY_ALIGNMENT, 4096);
diff --git a/hwy/base.h b/hwy/base.h
index db0449e707..dd49873978 100644
--- a/deps/v8/third_party/highway/src/hwy/base.h
+++ b/deps/v8/third_party/highway/src/hwy/base.h
@@ -476,7 +476,7 @@ HWY_API void ZeroBytes(void* to, size_t num_bytes) {
 #if HWY_ARCH_X86
 static constexpr HWY_MAYBE_UNUSED size_t kMaxVectorSize = 64;  // AVX-512
 #elif HWY_ARCH_RISCV && defined(__riscv_v_intrinsic) && \
-    __riscv_v_intrinsic >= 11000
+    __riscv_v_intrinsic >= 2000000
 // Not actually an upper bound on the size.
 static constexpr HWY_MAYBE_UNUSED size_t kMaxVectorSize = 4096;
 #else
@@ -492,7 +492,7 @@ static constexpr HWY_MAYBE_UNUSED size_t kMaxVectorSize = 16;
 #if HWY_ARCH_X86
 #define HWY_ALIGN_MAX alignas(64)
 #elif HWY_ARCH_RISCV && defined(__riscv_v_intrinsic) && \
-    __riscv_v_intrinsic >= 11000
+    __riscv_v_intrinsic >= 2000000
 #define HWY_ALIGN_MAX alignas(8)  // only elements need be aligned
 #else
 #define HWY_ALIGN_MAX alignas(16)
diff --git a/hwy/detect_targets.h b/hwy/detect_targets.h
index caca43098c..c6518af7c0 100644
--- a/deps/v8/third_party/highway/src/hwy/detect_targets.h
+++ b/deps/v8/third_party/highway/src/hwy/detect_targets.h
@@ -648,9 +648,9 @@
 #define HWY_BASELINE_AVX10_2_512 0
 #endif
 
-// RVV requires intrinsics 0.11 or later, see #1156.
+// Check for RVV 2.0, though 0.11 (11000) already worked.
 #if HWY_ARCH_RISCV && defined(__riscv_v_intrinsic) && \
-    __riscv_v_intrinsic >= 11000
+    __riscv_v_intrinsic >= 2000000
 #define HWY_BASELINE_RVV HWY_RVV
 #else
 #define HWY_BASELINE_RVV 0
diff --git a/hwy/ops/rvv-inl.h b/hwy/ops/rvv-inl.h
index 662463973e..3aeb1df167 100644
--- a/deps/v8/third_party/highway/src/hwy/ops/rvv-inl.h
+++ b/deps/v8/third_party/highway/src/hwy/ops/rvv-inl.h
@@ -1190,7 +1190,7 @@ HWY_RVV_FOREACH_I(HWY_RVV_RETV_ARGVV, SaturatedSub, ssub, _ALL)
 // Clang 16 with __riscv_v_intrinsic == 11000 may either require VXRM or avoid.
 // Assume earlier versions avoid.
 #elif HWY_COMPILER_CLANG && \
-    (HWY_COMPILER_CLANG < 1600 || __riscv_v_intrinsic < 11000)
+    (HWY_COMPILER_CLANG < 1600 || __riscv_v_intrinsic < 2000000)
 #define HWY_RVV_AVOID_VXRM
 #endif
 
