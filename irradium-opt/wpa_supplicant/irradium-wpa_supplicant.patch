--- a/wpa_supplicant	2025-11-16 06:09:53.040154266 +0200
+++ b/wpa_supplicant	2025-11-16 06:11:51.604154323 +0200
@@ -1,22 +1,26 @@
-#!/bin/sh
+#!/bin/bash
 #
 # /etc/rc.d/wpa_supplicant: start/stop wpa_supplicant
 #
 
-DEV=wlp3s0
+IFNAME=(wlan0)
 
 SSD=/sbin/start-stop-daemon
 PROG=/usr/sbin/wpa_supplicant
-PID=/run/wpa_supplicant.pid
-OPTS="-B -P $PID -D nl80211,wext -c /etc/wpa_supplicant.conf -i $DEV"
+OPTS="-B -D nl80211,wext -c /etc/wpa_supplicant.conf"
 
 
 case $1 in
 	start)
-		$SSD --start --pidfile $PID --exec $PROG -- $OPTS
+		for i in ${!IFNAME[@]}; do
+			PID=/run/wpa_supplicant.${IFNAME[$i]}.pid
+			$SSD --start --pidfile $PID --exec $PROG -- $OPTS -P $PID -i ${IFNAME[$i]}
+		done
 		;;
 	stop)
-		$SSD --stop --retry 10 --pidfile $PID
+		for i in ${!IFNAME[@]}; do
+			$SSD --stop --retry 10 --pidfile /run/wpa_supplicant.${IFNAME[$i]}.pid
+		done
 		;;
 	restart)
 		$0 stop
