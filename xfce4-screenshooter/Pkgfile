# Description: An application to take screenshots
# URL: https://docs.xfce.org/apps/xfce4-screenshooter/start
# Maintainer: mara, mara at fail dot pp dot ua
# Depends on: xfce4-panel libsoup3 hicolor-icon-theme

name=xfce4-screenshooter
version=1.10.3
release=1
source=(https://archive.xfce.org/src/apps/$name/${version%.*}/$name-$version.tar.bz2)

build(){
  cd $name-$version

  ./configure \
    --prefix=/usr \
    --sysconfdir=/etc \
    --libexecdir=/usr/lib \
    --localstatedir=/var \
    --disable-debug

  make
  make DESTDIR=$PKG install || exit 1

  rm -r $PKG/usr/share/locale

}
